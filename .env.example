# Kayser Downloads Backend - Configuración

# Puerto en el que corre el servidor (default: 3001)
PORT=3001

# API Secret para autenticación POST
# En desarrollo: kayser-downloads-secret-2024
# En producción: genera una clave segura con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_SECRET=kayser-downloads-secret-2024
